# Clerk
# Obtain keys from Clerk Dashboard -> API Keys, and copy JWT issuer domain from JWT template (for Convex auth).
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
CLERK_JWT_ISSUER_DOMAIN=
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
# New redirect envs (replaces deprecated AFTER_* vars)
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/dashboard
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/onboarding
# Optionally force redirect after auth (always redirects regardless of redirect_url)
# NEXT_PUBLIC_CLERK_SIGN_IN_FORCE_REDIRECT_URL=/dashboard
# NEXT_PUBLIC_CLERK_SIGN_UP_FORCE_REDIRECT_URL=/dashboard

# OpenAI
OPENAI_API_KEY=your-openai-api-key

# Stripe
STRIPE_SECRET_KEY=your-stripe-secret-key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key
STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret
# Payment Links (configure success_url in Stripe Dashboard to redirect to /onboarding after payment)
NEXT_PUBLIC_STRIPE_PAYMENT_LINK_MONTHLY=
NEXT_PUBLIC_STRIPE_PAYMENT_LINK_ANNUAL=

# Optional: provide these to enable dynamic price display in the UI
# Find them in Stripe Dashboard under Products -> Prices
STRIPE_PRICE_ID_MONTHLY=
STRIPE_PRICE_ID_ANNUAL=

# Email
SENDGRID_API_KEY=your-sendgrid-api-key
MAILGUN_SENDING_API_KEY=your-mailgun-sending-api-key

# Feature Flags
# Enable student organization badge in sidebar (shows university name)
# Set to "true" after backfilling existing students to new role scheme
NEXT_PUBLIC_ENABLE_STUDENT_ORG_BADGE=false