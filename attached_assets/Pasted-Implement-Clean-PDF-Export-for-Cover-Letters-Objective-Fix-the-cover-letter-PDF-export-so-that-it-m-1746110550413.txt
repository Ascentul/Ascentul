Implement Clean PDF Export for Cover Letters
Objective:
Fix the cover letter PDF export so that it mirrors the preview layout — with proper line spacing, font styling, paragraph breaks, and no browser-generated headers/footers.

🧩 Tasks to Complete
1. Wrap Cover Letter in Structured HTML Before Export
Instead of exporting plain text, export this structure:

html
Copy
Edit
<div id="coverLetterPreview" class="pdf-body">
  <p>[Your Name]</p>
  <p>[Date]</p>
  <p>Hiring Manager<br>Sercante</p>

  <p>Dear Hiring Manager,</p>

  <p>I’m excited to apply for the Marketing Automation Analyst role...</p>

  <p>Sincerely,<br>[Your Name]</p>
</div>
Use the current preview data from the app, but render it in export-ready format (you can reuse dangerouslySetInnerHTML if needed).

2. Apply PDF-Specific CSS
Create or scope the following CSS:

css
Copy
Edit
.pdf-body {
  font-family: Arial, sans-serif;
  font-size: 12pt;
  line-height: 1.6;
  color: #000;
  padding: 40px;
}

.pdf-body p {
  margin-bottom: 12px;
}

.placeholder {
  color: #aaa;
  font-style: italic;
}
✅ This ensures consistent, print-friendly formatting.

3. Use html2pdf.js to Generate the PDF
Install if needed:

bash
Copy
Edit
npm install html2pdf.js
Add this logic to the “Download PDF” button:

ts
Copy
Edit
import html2pdf from 'html2pdf.js';

const element = document.getElementById("coverLetterPreview");

html2pdf().set({
  margin:       0,
  filename:     'cover-letter.pdf',
  image:        { type: 'jpeg', quality: 0.98 },
  html2canvas:  { scale: 2 },
  jsPDF:        { unit: 'in', format: 'letter', orientation: 'portrait' }
})
.from(element)
.save();
✅ This creates a polished, formatted PDF with no browser print UI artifacts (no timestamps, no URL, no header/footer).

4. Remove Any Print Browser Headers/Footers
Don’t use window.print() — use html2pdf only

Ensure no <pre> tags are used (they collapse formatting)

Expected Result:

Exported PDF mirrors the exact preview

Line breaks and spacing match

Placeholder tokens are visible but styled softly if not filled

No page numbers, timestamps, or browser UI elements appear

