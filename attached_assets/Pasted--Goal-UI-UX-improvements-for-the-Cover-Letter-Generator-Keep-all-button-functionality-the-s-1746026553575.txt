// 🎯 Goal: UI/UX improvements for the Cover Letter Generator
// Keep all button functionality the same — do NOT change onClick behavior.

// ✅ 1. Visual hierarchy between action buttons
// - Make "Generate Cover Letter" a solid primary button (brand blue)
// - Make "Get Suggestions" a secondary button (outline or lighter tone)
// - Add horizontal spacing or a small divider between them

generateButton.classList.add('btn-primary'); // Ensure it's blue/filled
suggestionsButton.classList.add('btn-secondary'); // Outline or subtle fill
suggestionsButton.style.marginRight = "8px";

// ✅ 2. Add header above the generated cover letter
const previewHeader = document.createElement('div');
previewHeader.textContent = '📄 Draft Preview';
previewHeader.style.fontWeight = '600';
previewHeader.style.marginBottom = '8px';
previewHeader.style.fontSize = '14px';
previewHeader.style.color = '#333';

const previewContainer = document.getElementById('generatedCoverLetter');
previewContainer.prepend(previewHeader);

// ✅ 3. Improve explanatory UX copy under job description
const helperText = document.getElementById('ai-helper-text'); // Assuming this is the helper copy
helperText.textContent = "We’ve pulled your resume details (experience, education, skills) to tailor this cover letter for you.";
helperText.style.fontSize = "12px";
helperText.style.color = "#666";

// ✅ 4. Add loading state when generating a cover letter
generateButton.addEventListener('click', () => {
  generateButton.innerText = "Generating...";
  generateButton.disabled = true;

  // Simulate the existing generateCoverLetter function
  generateCoverLetter().then(() => {
    generateButton.innerText = "Generate Cover Letter";
    generateButton.disabled = false;
  });
});

// ✅ 5. Improve styling of the generated letter
previewContainer.style.border = "1px solid #e0e0e0";
previewContainer.style.borderRadius = "8px";
previewContainer.style.padding = "16px";
previewContainer.style.boxShadow = "0 2px 6px rgba(0,0,0,0.04)";
previewContainer.style.backgroundColor = "#fafafa";
previewContainer.style.lineHeight = "1.6";

// ✅ 6. Toast confirmation on "Save as Cover Letter"
saveButton.addEventListener('click', () => {
  // Existing save logic here...

  // Show toast message
  const toast = document.createElement('div');
  toast.innerText = "✅ Cover letter saved to ‘My Cover Letters’";
  toast.style.position = "fixed";
  toast.style.bottom = "20px";
  toast.style.right = "20px";
  toast.style.backgroundColor = "#333";
  toast.style.color = "#fff";
  toast.style.padding = "10px 16px";
  toast.style.borderRadius = "6px";
  toast.style.zIndex = "999";
  document.body.appendChild(toast);

  setTimeout(() => {
    toast.remove();
  }, 3000);
});
